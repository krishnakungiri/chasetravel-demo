import { CustomPipe1Pipe } from './custom-pipe-1.pipe';

describe('CustomPipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
